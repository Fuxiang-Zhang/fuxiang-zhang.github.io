name: Deploy homepage and vibe to Pages

on:
  push:
    branches: ["main", "feat/terminal-homepage"]
  workflow_dispatch:

permissions:
  contents: read

# Both branches publish one complete site, using the same deployment queue.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout original homepage
        uses: actions/checkout@v4
        with:
          ref: main
          path: production
          persist-credentials: false
      - name: Checkout terminal homepage
        uses: actions/checkout@v4
        with:
          ref: feat/terminal-homepage
          path: terminal
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: terminal/package-lock.json
      - name: Install terminal homepage dependencies
        working-directory: terminal
        run: npm ci
      - name: Check, test and build terminal homepage
        working-directory: terminal
        run: |
          npm run check
          npm test
      - name: Assemble complete site
        run: |
          mkdir -p _site
          rsync -a --exclude='/.git/' --exclude='/.github/' --exclude='/vibe/' production/ _site/
          mkdir -p _site/vibe
          cp -a terminal/dist/. _site/vibe/
          touch _site/.nojekyll
          test -f _site/index.html
          test -f _site/vibe/index.html
          test -f _site/vibe/data/site.md
      - name: Upload complete site
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
